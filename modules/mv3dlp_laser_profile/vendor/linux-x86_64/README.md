Place the vendor Linux runtime libraries here.

Recommended minimum file name:

```text
libMv3dLp.so
```

If the vendor SDK ships additional dependent `.so` files, place them in the same folder and export
`LD_LIBRARY_PATH` before running your application.
